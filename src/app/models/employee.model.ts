export class Employee {
    id!: number;
    firstName!: string;
    lastName!: string;
    salary!: number;
    zipcode!: number;

    constructor(id: number, firstName: string, lastname: string, salary: number, zipcode: number){
     this.id=id
     this.firstName= firstName
     this.lastName=lastname
     this.salary=salary
     this.zipcode=zipcode

      

    }
   
 
}
